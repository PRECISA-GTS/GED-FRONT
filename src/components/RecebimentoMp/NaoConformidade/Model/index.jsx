import { Card, CardContent } from '@mui/material'

const Model = ({ form }) => {
    return (
        <Card>
            <CardContent>Model</CardContent>
        </Card>
    )
}

export default Model
